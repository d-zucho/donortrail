import { SignIn } from '@clerk/nextjs'

/**
 * Render the Clerk SignIn UI for the /sign-in route.
 *
 * `@returns` A JSX element that renders the Clerk SignIn UI.
 */
export default function SignInPage() {
  return <SignIn />
}

  return <SignIn />
}